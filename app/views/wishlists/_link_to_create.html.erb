<%= link_to text.capitalize, {
  controller: 'wishlists',
  action: 'create',
  wishlist: {
    preference: 'yep',
    movie_id: movie.id,
    user_id: current_user
  }
}, method: :post, remote: true, class: 'add-btn' %>
